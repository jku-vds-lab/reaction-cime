export default function register(registry) {
  /*
  if (process.env.ENABLE_COOKIE_STORE) {
    registry.push(EXTENSION_POINT_CUSTOMIZED_LOGIN_FORM, 'api_cookie_store_login', () => import('./auth/SecurityCookieStoreLoginDialog'), {});
  }

  registry.push(
    EP_TDP_CORE_LINEUP_PANEL_TAB,
    'lineUpSettingsPanel',
    () => {
      return import('./app/LineupSettingsPanel').then((m) => m.LineupSettingsPanel);
    },
    <IPanelTabExtensionDesc>{
      cssClass: '',
      faIcon: 'fas fa-gear',
      title: 'LineUp Settings',
      order: 10,
      width: '24em',
      shortcut: true,
    },
  );
  */
}
